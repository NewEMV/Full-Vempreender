
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite a leitura pública de todos os arquivos
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null; // Apenas usuários autenticados podem escrever
    }
  }
}
